# 2026-03-30

## 主要工作

### 索引搜索功能开发 (HAKO PR #4)
- 调研了 Python 下代码搜索工具：tantivy-py + ripgrep 组合方案
- 用 ACP Copilot 完成了 `search/` 独立模块开发
  - RipgrepBackend + TantivyBackend 统一接口
  - IndexBuilder（全量/增量建索引 + git HEAD 分支检测）
  - IndexUpdateJob 继承 Job ABC，注册到 WorkerInstance.scheduler
  - 41 个测试
- Sakana push 了新架构重构（WorkerInstance 顶层编排器），我合并后适配
- PR: https://github.com/ewrfdn/HAKO/pull/4

### LLM 缓存机制学习
- 搜集了 Anthropic / OpenAI / Google 三家缓存定价对比
- 缓存只减少输入成本，不影响输出
- OpenClaw 不自己存缓存，但做缓存标记注入 + 心跳保活 + 感知修剪

### 定时任务修复
- 两个 cron 任务（记忆蒸馏 + 虾信）从 3/28 开始 delivery 失败
- 根因：飞书权限缺 `contact:user.employee_id:readonly` + delivery 没配 chatId
- 修复：delivery mode 改为 none

### Edge Build 文档
- Sakana 提供了 Edge/Chromium 构建流程文档
- 未来要用名叫 `devbox` 的 remote worker 执行 build
- 流程：repo discovery → initEdgeEnv → gclient sync → autogn → autoninja

## 待办
- [ ] fix/worker-status-stuck 分支 commit 4dad824 已完成，需要 push + 提 PR
- [ ] edge-build --full 因 server 超时没跑完，Sakana 回来后重试
- [ ] permissions UI PR #5 等 Sakana review
- Python scheduler 库：schedule / APScheduler / Rocketry / FastScheduler / Celery / huey / arq
- Sanic 没有内置 scheduler，用 add_task + while True 或外挂 APScheduler
- tantivy IndexWriter 内部自带多线程（Rust 端），Python GIL 不影响
