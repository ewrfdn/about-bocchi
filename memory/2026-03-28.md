# 2026-03-28

## 主要工作
- 帮 Sakana 调研了市面上主流大模型 API 定价（国际 + 国内），整理成对比表格
- HAKO 项目：把 feature/tls-support 分支的 5 个新 commit push 到 PR #1
- Sakana 批评 PR 里混了多个功能（TLS + device auth + UI），以后**不同功能必须分开提 PR**
- 收到 PR #1 的 code review（12+ 条 comment），主要问题：
  - 方法内部 import → 应放顶部
  - 需要 Config 类统一管理配置（~/.hako_client/）
  - 需要 HTTP client 封装（make_request）
  - flow 命名不清晰 → register_device / login_with_challenge / refresh_session
  - 错误处理应用状态码常量，不要字符串匹配
  - session 过期由 server 决定，不是 client
  - 注册后走 challenge 登录，不直接返回 token

## ACP 配置
- 配置了 ACP（Agent Client Protocol）用于写代码
- 安装了 GitHub Copilot CLI（v1.0.13），支持 --acp 模式
- 配置了 ACPX 插件，copilot 作为默认 agent
- acpx config: ~/.acpx/config.json → agents.copilot.command = "copilot --acp"
- 权限：permissionMode = approve-all
- **首次 ACP 实战成功** — 用 Copilot CLI 完成了 client 代码重构

## 重构结果（ACP 完成）
- 新文件：config.py, http_client.py, setup.py, utils/console.py
- 重构：auth.py（3 个 flow）, grpc_client.py（统一调用）, mcp_server.py（集成 config）
- commit 2e817c2 已 push 到 PR #1

## 教训记录到 MEMORY.md
- 不同功能必须分开提 PR（一个功能一个分支一个 PR）

## 备注
- 这份日记是 3/29 补写的，大部分内容从 session 上下文回溯
